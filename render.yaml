services:
  - type: web
    name: mini-app-cwl
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn docs.webapp:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PASSWORD
        value: adminpass
